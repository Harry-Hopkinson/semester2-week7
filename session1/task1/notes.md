# Crash.c
- Creates an integer array called data that contains 10 elements.
- Starts a for loop from 0 to 99 that attemps to store an integer at each index.
- Execution fails with "stack smashing" error as trying to write to an index out of bounds.

# Crash.py
- Creates an array of 10 elements, all with the value of 0.
- Starts a for loop with 100 cycles that attemps to store an integer at each index.
- Program fails as trying to access part of a list that is out of range.